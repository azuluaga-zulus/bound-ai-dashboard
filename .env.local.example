# N8N Configuration
NEXT_PUBLIC_N8N_BASE_URL=http://localhost:5678

# N8N Webhook Endpoints
# Estos son los endpoints que debes configurar en tu flujo de N8N
# 
# Webhook 1: Inicialización del chat
# URL: /webhook/onboarding-chat
# Method: POST
# 
# Webhook 2: Mensajes del chat  
# URL: /webhook/chat-message
# Method: POST (para enviar) / GET (para recibir)
#
# El flujo de N8N debería:
# 1. Recibir los datos del formulario en /webhook/onboarding-chat
# 2. Inicializar el LLM con esos datos
# 3. Devolver un sessionId único
# 4. Mantener el estado de la conversación por sessionId
# 5. Procesar mensajes en /webhook/chat-message
# 6. Devolver respuestas del LLM